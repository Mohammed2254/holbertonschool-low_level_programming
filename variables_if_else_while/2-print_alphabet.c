#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints the last digit of a random number
 * and states whether it is greater than 5, less than 6 and not 0, or 0.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
for ( i = 97; i < 123; i++)
putchar(i);
putchar('\n');
return (0);
}
